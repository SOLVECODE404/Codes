#include<iostream>

using namespace std;

int main(){
    // int n;
    // cin>>n;
    // int k;
    // cin>>k;
    // cout<<"a&b:"<<(k&n)<<endl;
    // cout<<(k<<1)<<endl;
    // cout<<(n>>2)<<endl;


// when we shifts the right (>>)one than
//  its divide with that term that much
//  time for one it diivdes with the 2 and for 
// rest square of themselves depending on the no of
//  times its got shifted.


// when we shift the left(<<) side one then it got multiplied 
// by 2 for one and rest is for that no
//  that much time in that period of time..




    cout<<(17<<1)<<endl;/*multiply by 2*/
    cout<<(17<<2)<<endl;/*square of 2 multiply*/
    cout<<(14>>1)<<endl;/*divide by 2*/
    cout<<(14>>2)<<endl;/*divide by 4*/
    cout<<(11>>7)<<endl;/*divide by 4*/

}